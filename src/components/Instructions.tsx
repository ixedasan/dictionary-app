type Props = {}

export const Instructions = (props: Props) => {
  return (
    <section className="mt-12 flex w-full items-center justify-center text-center">
      <p className="text-fs-sub-title">
        Type a word in the search bar and press 'Enter' to see the results.
      </p>
    </section>
  )
}
